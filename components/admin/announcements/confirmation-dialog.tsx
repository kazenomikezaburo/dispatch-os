"use client";

import { useEffect,useId,useRef,type ReactNode } from "react";
import { DialogFocusGuard,trapDialogFocus } from "@/components/admin/dialog-focus";

export function ConfirmationDialog({open,title,description,confirmLabel,pending,danger=false,onConfirm,onClose,children}:{open:boolean;title:string;description:string;confirmLabel:string;pending:boolean;danger?:boolean;onConfirm:()=>void;onClose:()=>void;children?:ReactNode}){const ref=useRef<HTMLDialogElement>(null);const returnFocus=useRef<HTMLElement|null>(null);const id=useId();const titleId=`announcement-confirm-title-${id}`;const descriptionId=`announcement-confirm-description-${id}`;useEffect(()=>{const dialog=ref.current;if(!dialog)return;if(open&&!dialog.open){returnFocus.current=document.activeElement instanceof HTMLElement?document.activeElement:null;dialog.showModal();}else if(!open&&dialog.open){dialog.close();returnFocus.current?.focus();}},[open]);return <dialog ref={ref} aria-labelledby={titleId} aria-describedby={descriptionId} onCancel={event=>{event.preventDefault();if(!pending)onClose();}} onKeyDown={event=>{trapDialogFocus(event);if(event.key==="Escape"&&!pending){event.preventDefault();onClose();}}} className="m-auto w-[calc(100%-2rem)] max-w-lg rounded-panel border border-border bg-surface p-0 text-foreground shadow-ds-overlay backdrop:bg-[var(--surface-overlay)]"><DialogFocusGuard edge="start"/><div className="p-5 sm:p-6"><h2 id={titleId} className="text-xl font-semibold">{title}</h2><p id={descriptionId} className="mt-2 text-sm text-foreground-secondary">{description}</p>{children&&<div className="mt-4 rounded-control border border-border bg-surface-subtle p-4 text-sm">{children}</div>}<div className="mt-6 flex flex-col-reverse gap-3 sm:flex-row sm:justify-end"><button autoFocus type="button" disabled={pending} onClick={onClose} className="min-h-11 rounded-control border border-border-strong px-5 text-sm font-medium hover:bg-surface-hover focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-focus-ring">キャンセル</button><button type="button" disabled={pending} onClick={onConfirm} className={`min-h-11 rounded-control px-5 text-sm font-semibold text-white focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-focus-ring disabled:bg-surface-muted ${danger?"bg-danger hover:opacity-90":"bg-primary hover:bg-primary-hover"}`}>{pending?"処理中…":confirmLabel}</button></div></div><DialogFocusGuard edge="end"/></dialog>;}
