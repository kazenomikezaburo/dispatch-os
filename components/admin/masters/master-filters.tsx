import Link from "next/link";import { adminStateActionClass } from "@/components/admin/admin-state";import { masterHref } from "@/lib/admin/masters/master-rules";import type { MasterQuery } from "@/lib/admin/masters/master-types";
const field="mt-1.5 min-h-11 w-full rounded-control border border-border-strong bg-surface px-3 text-sm focus-visible:outline-2 focus-visible:outline-focus-ring";
export function MasterFilters({path,query,placeholder}:{path:string;query:MasterQuery;placeholder:string}){return <form method="get" className="rounded-panel border border-border bg-surface p-4"><div className="grid gap-4 sm:grid-cols-[minmax(0,1fr)_12rem]"><label className="text-sm font-medium">検索<input type="search" name="q" defaultValue={query.q} maxLength={100} placeholder={placeholder} className={field}/></label><label className="text-sm font-medium">状態<select name="status" defaultValue={query.status} className={field}><option value="all">すべて</option><option value="active">有効</option><option value="inactive">無効</option></select></label></div><div className="mt-4 flex justify-end gap-3"><Link href={masterHref(path,{...query,q:"",status:"all",page:1})} className={adminStateActionClass}>条件をリセット</Link><button className="min-h-11 rounded-control bg-primary px-5 text-sm font-semibold text-primary-foreground">検索</button></div></form>}
