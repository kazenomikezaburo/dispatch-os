import "server-only";
import { createClient } from "@/lib/supabase/server";
import { PAGE_SIZE } from "./master-rules";
import type { ClientItem, MasterQuery, MasterResult, WorkplaceItem } from "./master-types";

export async function getClients(query:MasterQuery):Promise<MasterResult<ClientItem>>{try{const supabase=await createClient();let request=supabase.from("clients").select("id,branch_id,name,note,is_active,created_at,updated_at,projects(count)",{count:"exact"});if(query.q)request=request.ilike("name",`%${query.q}%`);if(query.status!=="all")request=request.eq("is_active",query.status==="active");const from=(query.page-1)*PAGE_SIZE;const [rows,branches,active]=await Promise.all([request.order("name").order("id").range(from,from+PAGE_SIZE-1),supabase.from("branches").select("id,name").order("name"),supabase.from("clients").select("id",{count:"exact",head:true}).eq("is_active",true)]);if(rows.error||branches.error||active.error)throw rows.error??branches.error??active.error;return{ok:true,total:rows.count??0,active:active.count??0,branches:branches.data??[],items:(rows.data??[]).map(row=>({id:row.id,branchId:row.branch_id,name:row.name,note:row.note,isActive:row.is_active,createdAt:row.created_at,updatedAt:row.updated_at,projectCount:(row.projects as unknown as {count:number}[])[0]?.count??0}))};}catch(error){console.error("Failed to load clients",error);return{ok:false};}}
export async function getWorkplaces(query:MasterQuery):Promise<MasterResult<WorkplaceItem>>{try{const supabase=await createClient();let request=supabase.from("workplaces").select("id,branch_id,name,postal_code,address,default_transport_note,access_note,meeting_note,is_active,created_at,updated_at,jobs(count)",{count:"exact"});if(query.q)request=request.or(`name.ilike.%${query.q}%,address.ilike.%${query.q}%`);if(query.status!=="all")request=request.eq("is_active",query.status==="active");const from=(query.page-1)*PAGE_SIZE;const [rows,branches,active]=await Promise.all([request.order("name").order("id").range(from,from+PAGE_SIZE-1),supabase.from("branches").select("id,name").order("name"),supabase.from("workplaces").select("id",{count:"exact",head:true}).eq("is_active",true)]);if(rows.error||branches.error||active.error)throw rows.error??branches.error??active.error;return{ok:true,total:rows.count??0,active:active.count??0,branches:branches.data??[],items:(rows.data??[]).map(row=>({id:row.id,branchId:row.branch_id,name:row.name,postalCode:row.postal_code,address:row.address,defaultTransportNote:row.default_transport_note,accessNote:row.access_note,meetingNote:row.meeting_note,isActive:row.is_active,createdAt:row.created_at,updatedAt:row.updated_at,jobCount:(row.jobs as unknown as {count:number}[])[0]?.count??0}))};}catch(error){console.error("Failed to load workplaces",error);return{ok:false};}}
